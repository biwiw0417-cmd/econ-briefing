name: daily-briefing

on:
  schedule:
    - cron: '37 21 * * *'  # UTC 21:37 = KST 06:37 (정각을 피해 혼잡 지연 최소화)
    - cron: '7 23 * * *'   # UTC 23:07 = KST 08:07 (첫 실행이 밀렸을 때 예비)
  workflow_dispatch:        # 수동 실행 버튼

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate briefing
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python scripts/generate.py

      - name: Build Word file
        run: python scripts/build_docx.py

      - name: Build site
        run: python scripts/build_site.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data docs
          git diff --cached --quiet || git commit -m "Daily briefing $(TZ=Asia/Seoul date +%F)"
          git push
